"""Command line entry point for {{ title }}."""

import typer

from {{ package }} import lib
from {{ package }}.schemas import {{ error_class }}

app = typer.Typer(
    no_args_is_help=True,
    pretty_exceptions_show_locals=False,
    context_settings={"help_option_names": ["-h", "--help"]},
)


@app.callback()
def cli() -> None:
    {{ description_docstring }}


@app.command()
def manifest() -> None:
    """Print the tool's manifest as JSON."""
    typer.echo(lib.load_manifest().model_dump_json(indent=2))


def main() -> int:
    try:
        app()
    except {{ error_class }} as error:
        typer.echo(error, err=True)
        return 1
    return 0


if __name__ == "__main__":
    raise SystemExit(main())

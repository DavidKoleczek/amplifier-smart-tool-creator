"""Top level entry point for the {{ title }} library."""

from {{ package }}.core import manifest
from {{ package }}.schemas import Manifest


def load_manifest() -> Manifest:
    """The tool's manifest as structured data, read from the SMART_TOOL.md shipped inside the package."""
    return manifest.load_manifest()

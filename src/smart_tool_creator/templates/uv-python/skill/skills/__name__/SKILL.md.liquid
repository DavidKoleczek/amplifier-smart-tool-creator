---
name: {{ name }}
description: >-
  {{ description }}. Drive it from the command line as `{{ name }}`, or from Python through
  `{{ package }}.lib`. Triggers on "{{ name }}".
license: MIT
---

# Using {{ name }}

{{ description }}.

## Install

From a clone of the repository:

```bash
# as a CLI
uv tool install .

# as a library, from another project
uv add /path/to/{{ name }}
```

## Use it

Run `{{ name }} --help`. It prints the tool's skill: when to use it, every capability,
worked invocations, sharp edges, and which files to read. Follow it. Confirm every argument
against `{{ name }} <command> --help` rather than memory.
